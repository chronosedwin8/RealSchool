# API — Plugins (Rule Engine y Scoring Engine)

SDK de plugins: `SchedulingPlugin`, `PenaltyTerm`, el registro, el Scoring Engine y el catálogo canónico de restricciones.

::: scheduling_platform.plugins
